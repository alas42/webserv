server {

	listen 80 default_server; // IPv4

	listen [::]:80 default_server; // IPv6 (Not used in the project)

	server_name example.com www.example.com;

	root /var/www/example.com;

	index index.html;

	try_files $uri /index.html;

	location /planet/blog {	}

}