#ifndef LISTEN_HPP
# define LISTEN_HPP

# include <iostream>
# include <string>

class Listen
{
	public:
		Listen(void);
		~Listen(void);
		Listen(const Listen & other);
		Listen & operator=(const Listen & other);

	private:

};

#endif
