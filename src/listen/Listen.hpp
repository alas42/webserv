#ifndef LISTEN_HPP
# define LISTEN_HPP

# include "./../../inc/webserv.hpp"

class Listen
{
	public:
		Listen(void);
		~Listen(void);
		Listen(const Listen & other);
		Listen & operator=(const Listen & other);

	private:

};

#endif
